[project]
name = "dnd3e-dm-manager"
version = "1.1.0"
description = "Python desktop DM manager for D&D 3E style games"
requires-python = ">=3.10"

dependencies = [
  "PySide6>=6.7"
]

[tool.pytest.ini_options]
testpaths = ["tests_py"]
